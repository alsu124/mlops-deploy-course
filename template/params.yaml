# Пока здесь только то, что нужно для генерации данных.
# По ходу курса этот файл вырастет: занятие 3 — гиперпараметры,
# занятие 5 — пути стадий, занятие 6 — настройки MLflow.

seed: 42

data:
  n_samples: 20000
  raw_path: data/raw/churn.csv
  processed_dir: data/processed
  test_size: 0.2
  val_size: 0.1

# Занятие 1: TODO - студент заполняет список признаков из baseline_notebook.py
# Структура: 3 типа признаков (numeric, categorical, binary)
# Подсказка: посмотри какие колонки какого типа в данных
features:
  numeric: []  # TODO: заполни числовые признаки
  categorical: []  # TODO: заполни категориальные признаки
  binary: []  # TODO: заполни бинарные признаки

# TODO (занятие 3): перенести сюда гиперпараметры модели
train:
  model: logreg
